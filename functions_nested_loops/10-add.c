#include "main.h"
#include <stdio.h>

/**
 * add - check the code
 * @first: first param
 * @second: second param
 *
 * Return: addition result of passed in params.
 */
int add(int first, int second)
{
	return (first + second);
}
